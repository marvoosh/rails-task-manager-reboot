<h1><%= @task.title %></h1>
<%= link_to "Edit this task", edit_task_path(@task), class: "link" %>

<div class="box">
  <h2 class="details"><%= @task.details %></h2>

  <% if @task.completed %>
    <h3>Completed 🙋</h3>
  <% else %>
    <h3>Not completed 😥</h3>
  <% end %>

  <%= link_to "All tasks", tasks_path, class: "link" %>
</div>
